registry=https://registry.npmjs.org/

package-lock=true        # keep the lockfile on
save-exact=true          # pin exact versions in package.json (no ^)
audit=true               # keep npm audit on by default
fund=false               # silence "funding" prompts in CI logs